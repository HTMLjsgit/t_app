<% if @posts.exists? %>
  <% if user_signed_in? %>
    <%= render partial: "payments/post_payment" %>
  <% end %>
<% end %>
<%= render partial: "posts/posts_list", locals: {posts: @posts} %>

<style>
  .container{
    max-width: 100%;
  }
</style>
